import React from "react";

const Spinner = () => {
  return (
    <div class="ui active dimmer">
      <div class="ui text loader">Allow to access your location</div>
    </div>
  );
};

export default Spinner;
